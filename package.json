{
  "name": "@evo/c-3po-doc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "gitbook serve",
    "build": "npm run install && gitbook build",
    "install": "gitbook install",
    "publish": "npm run build && cp CNAME _book && cd _book && git init && git commit --allow-empty -m 'update book' && git checkout -b gh-pages && touch .nojekyll && git add . && git commit -am 'update book' && git push git@github.com:c-3po-org/c-3po-doc.git gh-pages --force"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/c-3po-org/c-3po-doc.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/c-3po-org/c-3po-doc/issues"
  },
  "homepage": "https://github.com/c-3po-org/c-3po-doc#readme",
  "devDependencies": {
    "gitbook-cli": "^2.3.0"
  }
}
